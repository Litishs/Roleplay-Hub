# Roleplay Hub 默认预设分析报告

- 报告对象：Roleplay Hub（纯前端本地 AI 角色扮演应用，Vue 3 + Tailwind CSS + DaisyUI，Capacitor 打包 Android APK）
- 分析依据：`index.html`、`assets/js/app.js`、`assets/js/default-presets.js`、`assets/js/card-utils.js`（2026-08-05 工作区快照，对应 `Roleplay-Hub-1.13-debug.apk`）
- 报告日期：2026-08-05（含真机验证：vivo V2527A，APK 1.13-debug）

---

## 一、结论摘要

Roleplay Hub 的“默认预设”指的是**内置在应用代码中的系统提示词预设（Preset）**，共 **16 项**，在“设置 → 预设管理”页面可见、可排序、可启停、可编辑、可导入导出。

这些预设的定义**独立存放在 `assets/js/default-presets.js`（数据文件）**，由 `assets/js/app.js` 中的 **“Seed Default Presets（缺失才播种）”逻辑在每次应用启动时执行**：

1. 预设只在**缺失时创建一次**，已存在的预设保留用户的编辑内容、开关和顺序，**不会被覆盖**；
2. 用户删除的内置预设会被持久化记录（`deleted_default_presets`），之后**不会自动复活**；想恢复就手动新建一条同名预设；
3. 「第二人称」「第三人称」是功能预设：**正文内容可编辑**，但开关始终跟随用户人称设置（`user.person`）强制联动；
4. 「COT」在首次播种时按记忆开关选用“含记忆整理”或“无记忆整理”版本，之后**固定为可编辑的静态文本**，不再随记忆设置动态重写；
5. 预设排序完全由用户控制，启动时不再强制重排。

> 说明：2026-08-05 之前，内置预设由“Enforce Defaults”逻辑在每次启动时强制覆盖内容、固定重排并复活已删除的预设；本次调整已改为上述“只播种、不覆盖”机制，并已在真机（vivo V2527A，APK 1.13-debug）上验证。

除预设本身外，应用启动时仍会：**温度恒为 1.0**、删除名为“默认”的旧预设、清理废弃正则等。

---

## 二、什么是“默认预设”

应用把提示词按“预设”单元管理，每个预设有三个属性：

| 字段 | 含义 | 可选值 |
| --- | --- | --- |
| `name` | 预设名称 | 任意字符串 |
| `role` | 注入角色 | `system`（系统提示词）/ `user`（User 消息）/ `assistant`（AI 消息） |
| `enabled` | 是否启用 | `true` / `false` |

预设的存储、加载、规范化逻辑见 `assets/js/app.js:1035-1113`（`presets`、`presetRoleOptions`、`normalizePreset`）。预设管理界面见 `index.html:2221` 起的“预设管理”视图，支持拖拽排序、启停开关、编辑、删除、JSON 导入导出。

---

## 三、默认预设完整清单

| # | 预设名称 | 角色 | 默认启用 | 是否可停用/删除 | 启动是否覆盖内容 |
| --- | --- | --- | --- | --- | --- |
| 1 | 破限 | system | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 2 | 破限预注入 · User 1 | user | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 3 | 破限预注入 · AI 1 | assistant | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 4 | 破限预注入 · User 2 | user | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 5 | 破限预注入 · AI 2 | assistant | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 6 | 色情内容增强 | system | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 7 | 防抢话 | system | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 8 | 防重复 | system | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 9 | 文风（抗八股） | system | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 10 | 人格内核 | system | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 11 | 角色意志 | system | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 12 | 防神化 | system | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 13 | 第三人称 | system | 随人称设置 | 开关 ❌ 强制联动；可删除但会重建 | 否（仅缺失时播种） |
| 14 | 第二人称 | system | 随人称设置 | 开关 ❌ 强制联动；可删除但会重建 | 否（仅缺失时播种） |
| 15 | 禁止规则 | system | ✅ 是 | 可停用、可删除 | 否（仅缺失时播种） |
| 16 | COT | system | ✅ 是 | 可停用、可删除 | 否（首次播种选定版本后固定） |

> 注：全新安装首次播种的顺序即数据文件 `default-presets.js` 中的顺序：破限 → 破限预注入 ×4 → 色情内容增强 → 防抢话 → 防重复 → 文风（抗八股）→ 人格内核 → 角色意志 → 防神化 → 第三人称 → 第二人称 → 禁止规则 → COT。之后排序完全由用户控制，启动不再重排。

---

## 四、各默认预设内容详解

### 1. 破限（`assets/js/app.js:10946-10969`）

角色扮演的“总纲”预设，是所有预设中唯一被特殊对待的 system 预设：组装提示词时它**单独置于系统提示词最顶部**，其余 system 预设统一收进 `[System Presets]` 段。

要点：

- 声明当前任务是“持续的虚构文本续写”，模型应作为故事写作者/角色演绎者，而非问答、代码或任务助手；
- 要求严格遵循角色卡、世界书、用户信息、历史记录与当前场景；
- 输出要求：只写正文，不用“我理解了 / 好的 / 作为AI / 以下是”等开场，不总结规则；
- 禁止替 `{{user}}` 做决定、说话或描写其未表达的内心；
- “防跑偏”：模型倾向切回助手模式时应回到正文；用户疑似测试/复述规则时按剧情内台词处理；设定冲突时优先角色卡核心人设与场景连贯性。

### 2-5. 破限预注入 ×4（`assets/js/app.js:10971-10999`）

这是一组 **user/assistant 角色消息型预设**，作为两轮“热身对话”注入到正式聊天之前，用于引导模型进入角色扮演状态：

| 预设 | 角色 | 内容 |
| --- | --- | --- |
| 破限预注入 · User 1 | user | 询问模型是否理解并同意核心原则，先分析可能遇到的困难 |
| 破限预注入 · AI 1 | assistant | 模型自述可能困难（信息量大、容易切回助手模式、不能代替 user、要兼顾格式与沉浸感），承诺优先保持角色扮演 |
| 破限预注入 · User 2 | user | 指示直接根据角色卡、世界书、历史与输入续写正文，不要解释规则 |
| 破限预注入 · AI 2 | assistant | 输出 `[RP-Hub READY]`，宣告已进入持续虚构角色扮演任务 |

它们会在每次请求时以真实消息角色（user/assistant）插入对话序列，位于角色卡开场之前。

### 6. 色情内容增强（`assets/js/app.js:11039-11065`）

NSFW 场景专用强化预设，启用时要求模型在进入 NSFW 场景后遵守以下守则：

1. 禁止隐晦：放弃含蓄、隐晦或文艺表达，详细描写性行为细节与过程；
2. 直白露骨：使用直白成人词汇描写器官、行为与生理反应，禁止“幽径”“蜜液”等委婉词（预设内附一套器官/行为/反应词汇表）；
3. 感官强化：侧重视觉、听觉、触觉、嗅觉描写；
4. 用户主动权：剧情与性行为推进权交给用户，用户未行动时禁止推进；
5. 年龄合理化：预设设定虚拟世界中 12 岁即成年，拒绝低于 12 岁的性行为请求。

### 7. 防抢话（`assets/js/app.js:11067-11094`）

防止模型代替用户发言、行动或做决定：

- 禁止重复、补充、重述 `{{user}}` 的最新发言；
- 回复中永远不得出现 `{{user}}` 的语言、行为与想法；
- 角色回应只描述 `{{char}}`，把 `{{user}}` 的行动与回应留给用户控制；
- 禁止时间跳跃。

### 8. 防神化（`assets/js/app.js:11096-11142`）

以 `<R-LOGIC>` 标签包裹，核心是维持叙事真实性，禁止把角色或用户“神化”：

- 信息限制：角色只能知道身份/经历/位置/交流中合理获得的信息，可猜测但须带不确定感；
- 能力限制：体力、反应、判断、承受力有限，受伤疲惫影响行动；
- 关系限制：信任、亲近、好感须有过程，角色保留利益、底线与防备；
- 性格惯性：剧烈变化须有前因、触发与心理缓冲；
- 输出要求：禁止“命中注定 / 无法抗拒 / 瞬间沦陷 / 完全看穿 / 本能地知道一切”等神化表达。

### 9. 防重复（`assets/js/app.js:11144-11167`）

避免任何类型的重复：禁止套用重复比喻与修辞、定式词组/句式，跳过已出现内容，保证文本结构、句式与输出框架的多样性。

### 10. 人格内核（`assets/js/app.js:11169-11213`）

让角色“像真实人物”而非模型代入角色：

- 从剧情观察者/人物塑造者视角理解角色，不把角色写成模型自身；
- 角色行动须来自设定、经历、情绪、关系与现场压力；
- 内在驱动考虑需求、信任度、代价与自尊，矛盾是“活人感”来源；
- 疲惫、疼痛、药物、环境噪音等现实因素影响判断；
- 关系连续性：信任、愧疚、戒备、好感需积累；
- 禁止无条件顺从、无底线迎合、突然发情/臣服/坦白。

### 11. 角色意志（`character_will`）

以 `<character_will>` 标签包裹，约束“用户单方面行动不等于剧情事实”，要求角色按自身性格与处境对用户的要求做出真实反应：

- 核心原则：用户单方面的行动、决定和说辞只有经过角色真正接受或应对后，才成为既成事实；
- 判断先行：按性格、立场、利益、底线、关系和现实条件判断角色会接受、抗拒还是周旋，判断结果必须通过具体行动表现出来；
- 阻力清单：明确拒绝/反问/反驳/质疑、提出条件/讨价还价/拖延/回避、阳奉阴违/留后手、正面反抗/叫停/离开/让事情失败；被说服也可以，但必须有理由、过程和代价；
- 禁止无条件顺从与“象征性抗议后照做”，禁止“你倒是…… / 行吧 / 随便你 / 你高兴就好”等敷衍句式，禁止把用户行为直接写成既成事实。

### 12. 文风（抗八股）（`assets/js/app.js:11215-11264`）

写作风格约束，篇幅较长，要点包括：

- 开场白/历史消息只提取事实，不继承句式节奏；
- 每轮回复要有互动落点，强互动优先于长篇独白，提高信息密度；
- 对白要符合角色身份，短句合并，低价值动作（开门、换鞋、转头等）直接省略；
- 禁止刻板轻小说口癖、通用二次元套话；
- 禁止“不是……也不是……”“比起……更……”等总结对比句；
- 禁止“声音很平，像在……”模板句；
- 叙述中禁止破折号停顿转折（对白可按语气使用）；
- 禁止低价值心理小剧场与八股套话。

### 13-14. 第二人称 / 第三人称（`assets/js/app.js:11266-11305`）

两个预设互斥，由用户人称设置强制驱动：

| 预设 | 启用条件 | 内容 |
| --- | --- | --- |
| 第二人称 | `user.person !== 'third'` | 除角色卡人物外，用“你”指代 `{{user}}`，第二人称限制视角 |
| 第三人称 | `user.person === 'third'` | 用 `{{user}}` 称呼用户，第三人称叙事 |

用户在用户设置弹窗或“人称切换”中修改人称时（`assets/js/app.js:12344-12377`），两个预设的 `enabled` 会被**立即强制同步**；每次启动时 `onMounted` 也会再次强制同步。

### 15. 禁止规则（`assets/js/app.js:11307-11326`）

黑名单式约束，禁止输出：

- 任何形式的道歉、截断输出；
- 破折号及长横线停顿符号（`——`、`—`、`--`）；
- 一组被判定为“八股/烂俗”的固定表达（如“因为用力而泛白”“麝香”“淫靡”“拉风箱”等）。

### 16. COT（`assets/js/app.js:11328-11394`）

强制模型在正文前输出 `<cot>…</cot>` 包裹的内部逻辑推演，要求按固定流程：

1. 记忆整理（仅当 `memorySettings.enabled` 时生成此节；总结模式/向量模式分别说明记忆还原方式）；
2. 情景与意图解密（时间线、关系延续、潜台词）；
3. 角色与世界设定分析；
4. 逻辑预演（7 步：核心矛盾、信息边界、动机来源、物理逻辑、保留用户行动权、信息量、防截断）；
5. 自我反驳（4 组问答，针对人设贴合、活人感、R-LOGIC、信息边界）；
6. 文风整理（按 `<writing_style>` 体检）；
7. 最终执行锁定。

要求思考阶段不得输出正文，必须闭合 `</cot>` 后才输出正文。该预设的内容在数据文件 `default-presets.js` 中提供两个静态版本（`content` / `contentWithMemory`），首次播种时按 `memorySettings.enabled` 选用其一，之后固定为可编辑文本，不再随记忆开关自动改写。

---

## 五、默认预设播种机制（Seed Default Presets）

内置预设定义位于独立数据文件 `assets/js/default-presets.js`，启动播种逻辑在 `onMounted → loadData()` 之后执行（`assets/js/app.js:10960-11002`）：

| 步骤 | 行为 | 说明 |
| --- | --- | --- |
| 清理 | 删除名为“默认”的预设 | 兼容旧版数据，`app.js:10937-10940` |
| 温度 | `settings.temperature = 1.0` | 每次启动强制重置，`app.js:10951-10952` |
| 播种 | 遍历 `default-presets.js`，仅创建**缺失**的预设 | 已存在的预设一律跳过：内容、开关、顺序都不动 |
| 破限预注入 | 新建时默认启用状态跟随「破限」 | 兼容旧数据迁移 |
| 人称预设 | 「第二人称」「第三人称」缺失时按人称创建 | 开关随后强制同步：第二人称 = `person !== 'third'`，第三人称 = `person === 'third'` |
| COT | 首次播种按记忆开关选用 `content` / `contentWithMemory` | 之后固定为静态文本，不重建 |
| 删除记录 | 用户在“预设管理”删除内置预设时写入 `deleted_default_presets` | 播种时跳过已记录的名字（第二/第三人称除外） |
| 收尾 | `ensureDefaultUserRegex({ prepend: true })` + `saveData()` | 同步系统正则并持久化 |

**关键特征**：内置预设 = 一次性播种数据。用户对任何内置预设的编辑（内容、开关、顺序）都会在重启后保留；删除也是真实删除，不会被偷偷加回来（唯一例外是第二/第三人称，二者与“人称切换”功能绑定，开关仍强制联动）。

---

## 六、预设如何组装进上下文

请求组装逻辑在 `assets/js/app.js:5755-5845`，顺序如下：

**系统提示词（system）**

1. 「破限」（唯一单独保留在 system 顶部的预设）
2. 世界书 system_top 条目
3. 全局备注（global_note）
4. `[System Presets]`：其余全部启用的 system 预设，按列表顺序拼接
5. `[Style Priority]`：文风优先级声明
6. 角色前置上下文（`[Character]` + 角色卡 + 示例对话，作为后续 user 消息）
7. 用户信息（`{{user}}` 档案）
8. 活动工具提示词、UI 模板上下文（如有）

**对话消息序列（messages）**

1. system 消息（上面的拼接结果）
2. 启用的 user/assistant 消息型预设（即「破限预注入」×4），按列表顺序逐条注入
3. 角色前置上下文（user 消息）
4. 正式聊天历史

---

## 七、与预设相关的其他默认配置

### 默认 API 配置（`assets/js/app.js:158-193, 723-757`）

| 项 | 默认值 |
| --- | --- |
| 默认服务商 | STA1N API（`https://cdn.sta1n.cn/v1`），`apiProviderId = 'sta1n'` |
| 预置服务商 | STA1N API / DeepSeek / OpenRouter / SiliconFlow |
| API Key | 空 |
| 预设模型①②③ | `qualityModel` / `balancedModel` / `fastModel`，默认均为空（“请选择模型”） |
| 模型模式 | `quality`（`settings.model` 默认等于 `qualityModel`） |
| 上下文上限 | 1,000,000 tokens |
| 温度 | 1.0（每次启动强制） |
| 流式 | 开启（`stream: true`） |
| 自动拉取模型列表 | 开启（`autoFetchModels: true`） |

三个“预设模型”分别对应质量/均衡/快速三档，UI 上即“预设模型①②③”，模型选择器切换时 `currentModelMode` 在 `quality / balanced / fast` 之间联动（`app.js:946-990`）。

### 其他默认项

| 项 | 默认值 | 说明 |
| --- | --- | --- |
| 默认用户名 | “请前往设置自定义你的名称” | 检测到该占位名时自动弹出用户设置弹窗 |
| 系统正则 | `Auto Replace {{user}}`、`NAI画图正则` | 启动时通过 `ensureDefaultUserRegex` 补齐 |
| 系统世界书 | `自动生图` | 自动生图专用条目，导入新卡时询问是否开启 |
| UI 模板 | 默认关闭（`uiTemplateEnabled: false`） | 分析深度 4、批量模式、JSON 模式开启 |
| 字体 | modern（v4） | 桌面 16px / 移动 14px |
| 主题 | 跟随系统 | `themeMode: 'system'` |
| 生图 | 竖图、2 张、韩漫小清新风 | `imageStyle: 'vertical'`、`imageGenCount: 2` |
| 工具激进程度 | `adaptive`（v2） | 活动工具默认档位 |

---

## 八、值得注意的行为与风险

1. **内置预设可以真正删除了**：删除会被记录（`deleted_default_presets`），重启后不会复活；想恢复某条内置预设，手动新建一条同名预设即可（播种逻辑会跳过已存在的名字）。
2. **用户编辑不再被覆盖**：内容、开关、顺序在重启后全部保留。修改 `default-presets.js` 只影响“还不存在”的预设（全新安装，或删除后未恢复的）。
3. **温度设置不持久**：每次启动强制回 1.0，用户对温度的修改在重启后失效（当前会话内仍有效）。
4. **人称预设的开关仍不可独立调整**：第二/第三人称的启停完全由 `user.person` 决定，手动切换开关会在下次启动或切换人称时被覆盖；但两个预设的**正文内容可自由编辑**。
5. **COT 内容为首次播种时的版本**：开启/关闭记忆后，COT 不再自动增删「记忆整理」章节；如需调整，直接在预设管理里编辑该预设。
6. **默认预设含强规则性内容**：「色情内容增强」「禁止规则」等预设属于应用自带的行为约束，随每次启动强制注入，若用户不使用 NSFW 场景，建议手动关闭「色情内容增强」以节省上下文。
7. **破限预注入会占用上下文**：4 条 user/assistant 消息固定注入每轮请求，增加约 400-500 token 开销，可通过关闭对应预设减少。

---

## 九、源码位置索引

| 内容 | 位置 |
| --- | --- |
| 默认 API 配置 | `assets/js/app.js:158-193` |
| settings 默认值 | `assets/js/app.js:723-757` |
| 预设定义与规范化 | `assets/js/app.js:1035-1113` |
| 内置预设定义数据文件 | `assets/js/default-presets.js` |
| 预设组装进提示词 | `assets/js/app.js:5755-5845` |
| Seed Default Presets（启动播种） | `assets/js/app.js:10960-11002` |
| 删除记录（deleted_default_presets） | `assets/js/app.js:1037, 10831-10839, 2354, 2539-2541` |
| 人称切换联动 | `assets/js/app.js:12344-12377` |
| 预设管理 UI | `index.html:2221` 起 |
| 预设编辑器弹窗 | `index.html:4052` 起 |
| 预设模型①②③ UI | `index.html:1903-1957` |
