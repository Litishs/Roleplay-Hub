# Roleplay Hub 导入导出功能审计报告

审计日期：2026-08-04

## 1. 导出路径结论

改造前，普通导出会先写入 Android 应用缓存目录 `CACHE/exports/<时间戳>-<文件名>`，再打开系统分享面板。该目录属于应用私有缓存，不是稳定的用户下载目录，系统也可能自动清理，所以用户很难在文件管理器中直接找到原文件。

改造后，Android 普通导出和完整备份都使用系统“创建文档”选择器。用户可在每次导出时自行选择下载、文档、SD 卡或已接入的云盘目录，也可以修改文件名。只有目标文件实际写入成功后，App 才提示导出成功；取消选择不会生成缓存副本，也不会提示成功。

浏览器版仍使用浏览器下载机制。网页受浏览器安全策略限制，保存位置由浏览器的“每次下载前询问保存位置”等设置决定。

## 2. 功能清单

| 功能 | 格式 | 当前行为 |
| --- | --- | --- |
| 角色卡导入（主界面） | `.json`、`.png` | 读取 V1/V2 风格字段；PNG 读取 `chara` 元数据；导入世界书、正则、UI 模板；创建新 UUID 并立即选中角色 |
| 角色卡导出（主界面） | `.json`、`.png` | 导出名称、描述、性格、首条消息、作者备注、世界书、正则和 UI 模板；PNG 将相同数据写入 `chara` 文本块 |
| 聊天记录导入 | `.jsonl` | 每个非空行解析为一条消息；导入到当前选中角色，并整体替换其现有聊天记录 |
| 聊天记录导出 | `.jsonl` | 每条消息输出为一行 JSON；优先按角色 UUID 读取，兼容旧的数组下标存储键 |
| 预设导入/导出 | `.json` | 导入时追加并标准化；导出时可多选，输出对象数组 |
| 正则导入/导出 | `.json` | 兼容 `scriptName/findRegex/replaceString/disabled` 等外部字段；导入时追加；导出时可多选 |
| 世界书导入/导出 | `.json` | 接受数组、`entries` 数组或对象；导入时追加；导出为 `{ "entries": [...] }` |
| UI 模板导入/导出 | `.json` | 接受数组或 `templates`；按作用域写入全局或当前角色，重新生成 ID；导出为 RP Hub 模板包装格式 |
| 记忆导入/导出 | `.json` | 分别支持“总结模式”和“向量模式”；总结模式按来源去重，向量模式只接受带向量数据的分片 |
| 角色工坊导入/导出 | `.json`、`.png` | 可导入/导出角色卡，并单独导入/多选导出世界书和正则；普通角色卡导出不包含 UI 模板 |
| 完整备份/恢复（Android） | `.rphub-backup` ZIP | 备份 SQLite 与本地媒体，带清单和 SHA-256；恢复前校验路径、哈希和数据库完整性，失败时回滚 |

## 3. 数据范围与安全边界

- 完整备份包含角色、聊天、记忆、设置和 App 私有媒体文件。
- API Key 等密钥存放在 Android 加密存储中，不进入完整备份；恢复后会清空现有密钥，需要用户重新填写。
- 普通角色卡、聊天、记忆和配置导出都是明文文件，不加密。选择公共目录或云盘时应视为用户主动公开到该位置。
- 完整恢复会替换当前数据库和媒体；界面已要求二次确认，恢复实现包含校验、暂存和失败回滚。

## 4. 已发现问题与建议

### 高优先级

1. 主界面角色卡往返是有损的。导入时会主动删除 `mes_example`、`system_prompt`、`post_history_instructions`、`alternate_greetings`、`tags`、`creator`、`character_version`、`spec`、`spec_version`，导出结构也不包含这些字段。外部 SillyTavern 角色卡经过一次导入再导出后会丢失这些内容。角色工坊则保留其中一部分，两个入口行为不一致。建议统一数据模型并提供兼容字段保留策略。

2. 聊天 `.jsonl` 导入会直接整体覆盖当前角色聊天，没有覆盖确认、备份点或消息结构校验。建议导入前展示目标角色、消息数和“覆盖/追加”选项，并验证每条消息的角色和内容字段。

### 中优先级

3. 预设、正则和世界书导入采用追加策略，没有重复检测；重复导入同一文件会产生重复项。建议按稳定 ID 或内容指纹提示跳过、覆盖或保留副本。

4. 主界面角色 JSON 判断主要依赖 MIME `application/json`，部分 Android 文件提供器可能只返回空 MIME，即使文件名是 `.json` 也会被拒绝；角色工坊已兼容扩展名判断。建议主界面同步兼容扩展名。

5. 普通导出通过 JS/Base64 跨原生桥传递完整文件。对一般 JSON/PNG 足够，但非常大的头像或聊天文件会产生额外内存峰值。建议未来改为分块写入或临时文件 URI 流式复制。

### 低优先级

6. 完整备份选择器使用 ZIP MIME，但建议统一文件名为 `.rphub-backup.zip`，便于第三方文件管理器识别，也与仓库示例文件保持一致。

7. 独立 JSON 导入普遍只显示格式错误或成功数量，缺少导入前预览和冲突摘要。数据量较大时，建议增加校验结果页。

## 5. 本次改造覆盖范围

所有调用统一 `downloadBlob` 的 Android 导出现在都会弹出自定义保存位置，包括：主界面角色 JSON/PNG、聊天 JSONL、预设、正则、世界书、UI 模板、两种记忆，以及角色工坊的角色卡、世界书和正则导出。完整备份原本已使用系统选择器，保持不变。
