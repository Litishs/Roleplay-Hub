# Roleplay Hub 中文输入问题评估报告

评估日期：2026-08-04
评估对象：当前源码（HEAD a87c925，工作区干净）与真机上的 1.0 debug 包（设备 10AG5F0U0K005JF，13:56 安装，进程运行中）
默认输入法：讯飞输入法（com.iflytek.inputmethod/.FlyIME）——中文合成（setComposingText/commitText）重度依赖 IME

## 0. 结论摘要

中文输入"能用但不完整"。根因不在前端某一段代码，而在 **Android 原生层配置**：

- `capacitor.config.json` 里开启了 `"captureInput": true`，Capacitor 会用 `BaseInputConnection(this, false)`（dummy 模式）替换 WebView 原生输入连接；
- CJK 输入法提交合成文本时不再走 WebView 原生编辑路径，而是被 Capacitor 拦截成 `ACTION_MULTIPLE` 按键事件，用 JS 直接追加到 `document.activeElement.value`；
- 这个 JS 追加方案有四个副作用：**不触发 input/composition 事件**、**只在主文档的 value 型输入框生效**（iframe / Shadow DOM 内无效）、**追加到末尾而不是光标处**、**不显示合成下划线**。

为了绕开它，前端已经打了两层补丁（主聊天框 textarea + composition 守卫；iframe 卡片与 Shadow DOM 卡片的 IME 代理输入框）。这些补丁让"在输入框末尾连续打中文后直接发送/提交"这类最常见路径可用，但**光标定位、事件完整性、卡片重渲染、角色工坊 v-model 字段、Enter 确认候选词**等场景仍然有丢字/错位/不更新的风险。详见下文逐项评估。

一句话建议：**先在真机上做一次 `captureInput: false` 的 A/B 对比**。若原生 IME 恢复正常，前端这些代理桥应当整体下线，而不是继续打补丁。

## 1. 输入面清单

| # | 输入面 | 载体 | IME 处理现状 | 风险 |
| --- | --- | --- | --- | --- |
| 1 | 角色聊天框（主聊天输入） | `index.html` `<textarea ref="inputBox">`（无 v-model，DOM 为唯一真源） | 有 compositionstart/end、keydown(229 守卫)、paste、blur 同步 | 低-中：末尾输入可用；光标不在末尾/混输时错位；合成期间无 input 事件导致不自适应高度 |
| 2 | 角色回复富文本网页卡片（executable-html iframe） | `app.js` `setupIframeImeBridge` 主文档透明代理输入框 | 有代理桥 | 中-高：见 §3.1 缺口 a-f |
| 3 | 角色回复富文本网页卡片（Shadow DOM UI 模板） | `ui-template-frame.js` `setupImeBridge` 主文档透明代理输入框 | 有代理桥 | 中-高：与 iframe 桥同款缺口，且 inline `oninput` 重渲染会让宿主引用失效 |
| 4 | 角色工坊各类字段（名称/描述/性格/首条/世界书/正则/UI 模板等） | `character/index.html` 大量 `v-model` textarea/input | 无任何 composition 处理 | 中-高：JS 追加不触发 input 事件 → Vue model 不更新 → 重渲染/切字段丢字 |
| 5 | 角色工坊"描述你想创建的角色"prompt（Enter 发送） | `character/index.html:1335-1338` `@keydown.enter.exact.prevent="generateCharacter"` | 无 composition 守卫 | 中：输入法候选词回车可能误触发"生成角色" |
| 6 | 设置页 API Key | `index.html:1854-1856` + `syncApiKeyInput`（compositionend/blur/pointerdown） | 有同步 | 低 |
| 7 | 首次引导用户名 | `index.html:3511-3545` + `syncUserSetupName`（compositionend/change） | 有同步 | 低 |
| 8 | 编辑消息、临时系统指令 textarea | `index.html:574-577`、`931-933` 纯 v-model | 无处理 | 中：同 #4 |

## 2. 角色聊天框输入功能（输入面 #1）

### 2.1 实现方式
- 输入框是 `<textarea>`，**不绑定 v-model**，页面把 DOM 当作唯一数据源（`index.html:1028-1034`）。
- `app.js` 围绕它做了完整接线（约 447-560 行）：
  - `handleChatInput`：`event.isComposing || chatInputComposing` 时直接跳过，否则 rAF 内 `syncChatInputFromElement` + `autoResizeInput`；
  - `handleChatCompositionStart/End`：维护 `chatInputComposing` 标志，组合开始取消挂起的 rAF，组合结束补一次同步；
  - `handleChatInputKeydown`：Enter 发送，但 `isComposing || chatInputComposing || keyCode === 229` 时放行（不发送）；
  - `handleChatInputPaste`：`execCommand('insertText')` 插入纯文本并同步；
  - 发送按钮 `@pointerdown="prepareChatInputSend" @click="sendMessage"`，发送前强制同步一次 DOM。
- `sendMessage`（`app.js:4848` 起）读 `syncChatInputFromElement().trim()`，所以**只要 DOM 里看得见字，发送就能带出去**。

### 2.2 在 captureInput=true 下的实际行为
中文经原生层 JS 追加直接写进 `textarea.value`（末尾），**不触发 input 事件**，因此：
- 打字过程中 `userInput` 状态、自动增高、溢出滚动都不会更新；要到 `compositionend`/发送时才补一次同步；
- 如果光标不在末尾（修改中间文字、中英混输），中文会被追加到末尾，出现"字跑到最后"的错位；
- 组合中按回车确认候选词：`keydown` 的 229/isComposing 守卫能挡住大部分误发送，但由于 JS 追加路径下 WebView 未必正常派发 composition 事件，`chatInputComposing` 存在为 false 的边界，仍有误发送风险。

### 2.3 评估
常见路径（点进输入框 → 末尾连续打中文 → 回车/点发送）可用，这部分之前的修复（textarea 迁移 + 守卫）是有效的；但"编辑中间、混输、实时高度、组合状态"不完整。

## 3. 角色回复富文本网页卡片的输入功能（输入面 #2、#3）

角色回复里的富文本卡片有两种渲染通道，各自带一个 IME 代理桥：

### 3.1 executable-html iframe 卡片（`app.js` `setupIframeImeBridge`，3280-3448 行）
原理：Android WebView 下 iframe 内 `<input>` 能弹键盘、能输英文数字，但中文 IME 合成不进 iframe；同时主文档收不到 iframe 的 focus 事件。因此：
- `MutationObserver` 监听 iframe 入树，挂桥（`ensureIframeImeBridge`）；
- iframe 内 input/textarea 获焦（contentDocument focusin）时，在主文档**原位置**创建一个透明代理输入框（`data-rph-ime-proxy`，position:fixed + z-index 2147483647），并抢焦（`createProxy`，3334 行）；
- 用户在代理框输入（此时 `document.activeElement` 是主文档元素，JS 追加路径生效），`input/change` 时 `syncBack`（3325 行）把值写回 iframe 原输入框并派发 bubbles input 事件；
- blur 时同步一次并移除代理；scroll/resize 时重新对齐位置（600ms 防抖）。

优点：把"主文档 activeElement"这条唯一可行的输入通道利用了起来，实测（历史 tmp-ime 脚本）英文/ASCII 输入可进入卡片内 input。

缺口（也是"中文输入问题还在"的候选来源）：
- **a. 只覆盖 input/textarea**：`isIframeEditable`（3269 行）不认 contenteditable，富文本编辑器类输入框无代理；
- **b. 不转发 composition 事件**：`syncBack` 只 set `.value` + 派发 input，iframe 内脚本监听 compositionend 收不到，组合状态丢失；
- **c. Enter 被吞**：代理 keydown 里 `e.stopPropagation()`，且仅对 input 触发 blur；卡片内"回车提交"逻辑永远收不到 Enter（组合确认回车更不用说）；
- **d. 重渲染后宿主引用失效**：卡片脚本（如 Vue）收到 input 后重渲染替换原输入框时，代理持有的 `hostEl` 是游离旧节点，后续输入写到旧节点，**字从界面上消失**；桥没有 iframe 内 MutationObserver 重建代理；
- **e. 对齐脆弱**：依赖 scroll/resize 监听 + 600ms 防抖，卡片内滚动/键盘弹起的瞬间可能短暂错位；
- **f. 观感差异**：代理只复制颜色/字体/字号/对齐/行高，placeholder、背景、圆角等不一致（透明覆盖层）。

### 3.2 Shadow DOM UI 模板卡片（`ui-template-frame.js` `setupImeBridge`，226-362 行）
同一思路：shadowRoot focusin 捕获 → 主文档透明代理 → `syncBack`（287 行）写回 shadow 内元素并派发 `composed: true` 事件，让 `createInlineDelegate`（168-192 行）的 inline `oninput` 也能触发。

缺口与 3.1 的 a-f 基本一致；另外 shadow 模板的 inline handler 往往会改状态触发重渲染，**hostEl 失效问题在 Shadow DOM 路径下更常见**（模板脚本持有自己的数据模型，收到 input 后重绘面板）。

### 3.3 评估
代理桥是当前架构下合理的绕行方案，覆盖了"卡片里有一个普通 input/textarea"的常见模板；但卡片内输入框一旦参与脚本重渲染、或模板用 contenteditable、或依赖 Enter/composition 事件，就会暴露上述缺口。**这大概率就是"角色回复富文本网页卡片里中文输入还有问题"的直接原因**。

## 4. 原生层根因（贯穿所有输入面）

- `capacitor.config.json`：`"android": { ..., "captureInput": true, ... }`（b4392c5 引入，早于全部 IME 修复）。
- `CapacitorWebView.onCreateInputConnection`（node_modules @capacitor/android 7.6.8）：`captureInput=true` 时返回 `new BaseInputConnection(this, false)`，绕过 WebView 原生 `WebViewInputConnection`。
- `CapacitorWebView.dispatchKeyEvent`：`ACTION_MULTIPLE` 分支执行 `evaluateJavascript("document.activeElement.value = document.activeElement.value + '" + event.getCharacters() + "';")` 后返回 false。
- 结果：CJK IME 的合成文本不进入 WebView 编辑管线，而是变成按键事件 → JS 追加。默认输入法正是讯飞（合成重度），症状最明显。
- 这不是 WebView 版本问题（minWebViewVersion 83 以上），而是应用配置问题；Capacitor 社区同类问题通常以"captureInput 破坏 CJK IME"为讨论点。
- 前端所有补丁（textarea 守卫、两套代理桥、v-model compositionend 同步）都是在为这一个原生开关兜底，且都只能兜住部分路径。

## 5. 其它输入面（等等）

- **角色工坊（#4）**：全部 `v-model` 字段无 composition 处理。captureInput=true 下 JS 追加不派发 input 事件 → Vue model 不更新；字段失焦/重渲染时 DOM 会被 Vue 按旧 model 拉回，**中文内容丢失**。这是工坊编辑中文最容易翻车的点。
- **工坊 prompt（#5）**：`@keydown.enter.exact.prevent="generateCharacter"` 无 229/isComposing 守卫，候选词回车存在误触发"生成角色"的边界。
- **API Key（#6）/ 首次引导（#7）**：已有 compositionend/blur/pointerdown 同步，风险低（对应测试 user-setup-contract / api-key-input-contract 通过）。
- **编辑消息 / 临时系统指令（#8）**：纯 v-model，与 #4 同类风险。

## 6. 建议（按优先级）

### P0：真机 A/B 关闭 `captureInput`
1. 把 `capacitor.config.json` 的 `"captureInput": true` 改为 `false`（Capacitor 默认即 false），重新 `npm run android:debug` 装机；
2. 用讯飞/Gboard 分别测试：主聊天框中文输入（含中间编辑、回车确认候选词）、卡片内输入、工坊字段、API Key；
3. 若原生 IME 正常：整体下线 iframe 桥与 Shadow DOM 桥（保留或简化主聊天框 composition 守卫），并把工坊 v-model 字段补上 compositionend 同步；删除 `dispatchKeyEvent` 相关依赖。
4. 若 `captureInput=false` 会回归当初的问题（需确认当初为何开这个开关，可能是硬件键盘或多字符粘贴），再回到 P1 路线。

### P1（若必须保持 captureInput=true）
- 两套代理桥补 **MutationObserver**：iframe/shadow 内输入框被替换时自动重建代理（解决 d 缺口）；
- 支持 **contenteditable** 或在卡片模板规范里明确禁用并提示（解决 a）；
- 代理**转发 composition 事件**、Enter 在组合中吞掉/组合外转发给宿主（解决 b/c）；
- 主聊天框在 compositionend 后补一次 rAF 同步与 resize（现有逻辑已做，需真机确认）；
- 工坊 prompt 加守卫：组合中放行 `keydown.enter`，避免误生成（解决 #5）。

### P2（工程化）
- 把"IME 安全输入"抽成公共指令/组件，统一覆盖聊天、工坊、设置；
- 真机回归：录制一段"CDP 触焦 + adb 输入 + 读取 DOM/model"的自动化脚本（复用 .trae/tmp-ime-* 思路）纳入 `npm test` 之外的真机冒烟。

## 7. 附录：现状核验

- 源码与 dist 一致：`dist/index.html`、`dist/assets/js/app.js` 与源码关键标记（`handleChatCompositionStart`、`IME_PROXY_ATTR`、`chat-input-box`）匹配，13:55 构建，13:56 装机。
- 测试：`npm.cmd test` 50/50 通过（含 chat-input-ime-contract、rich-text-iframe-contract、iframe-lifecycle、user-setup-contract、api-key-input-contract）。注意：这些是**契约测试**，只验证"接线存在"，无法验证真机 IME 行为——不能作为"中文输入已修好"的证据。
- 关键文件与行号：
  - `capacitor.config.json`（captureInput: true）
  - `index.html:1028-1034`（聊天 textarea）、`574-577`（编辑消息）、`931-933`（临时指令）、`1854-1856`（API Key）、`3511-3545`（引导）
  - `assets/js/app.js`：`447-560`（聊天输入接线）、`3262-3448`（iframe IME 桥）、`4848-4880`（sendMessage）
  - `assets/js/ui-template-frame.js`：`168-192`（inline 委派）、`226-362`（Shadow DOM IME 桥）
  - `character/index.html`：`572-578`（diff）、`1335-1338`（prompt Enter 发送）、`941-987`、`1035-1239` 等（各类 v-model 字段）
  - `node_modules/@capacitor/android/.../CapacitorWebView.java`（onCreateInputConnection / dispatchKeyEvent）

## 8. P0 A/B 实测结果（2026-08-04）

> 实验：仅翻转 `capacitor.config.json` 的 `captureInput`（true→false），前端代码零改动，真机（讯飞输入法）重装对比。

### 8.1 方法
- 设备 10AG5F0U0K005JF，两种配置各构建+安装+启动；
- 用真实 adb 触摸（物理坐标=CSS×3.5+状态栏133）聚焦主聊天输入框，待软键盘弹出后 `adb shell input text "HelloAB"` 注入按键，读取 `textarea.value`，各跑 2 次；
- 用 CDP `Input.insertText` 模拟 IME 提交文本（触发 input 事件），验证 JS 层对"提交式输入"的处理；
- 卡片输入框：滚动到可见后真实点击，验证 IME 代理桥是否创建并回同步。

### 8.2 结果

| 项目 | captureInput=true（原配置） | captureInput=false（A/B） |
| --- | --- | --- |
| 真实点击聚焦聊天框 | ✅ | ✅ |
| 软键盘弹出 + 视口压缩 | ✅ | ✅ |
| adb 注入按键 "HelloAB"（键盘打开时） | ✅ 完整 "HelloAB"（2/2） | ❌ 丢字 "HAB"（2/2 复现） |
| CDP 提交式文本（insertText "测试中文"） | 未测 | ✅ 正确落位 + input 事件触发 |
| 卡片输入框 IME 代理桥 | 历史可用 | ✅ 代理创建、抢焦、同步回 iframe |
| 键盘弹出后输入框可见性 | ✅ | ✅（`--app-visual-height` 正常应用） |

### 8.3 结论
1. `captureInput=false` 恢复了 WebView 原生输入连接（不再被 `BaseInputConnection(this,false)` dummy 模式替换），从机制上修复 CJK 合成路径；CDP 提交式文本与 input 事件均正常，说明 JS 层接线正确。
2. 但 `captureInput=false` 引入一个回归：**软键盘打开时，注入/实体键盘按键事件会被 IME 连接吞掉**（`adb input text` 丢字，HelloAB→HAB；true 下同场景完整落字）。该回归影响实体键盘（蓝牙/OTG）与自动化注入；对屏上软键盘输入无影响（软键盘走 commitText 而非 key event）。
3. 关键未决项：**真实讯飞中文合成（拼音→候选→提交）是否顺畅，需人工在真机验证**——这是本 A/B 的决定性测试，自动化无法替代。

### 8.4 当前状态与下一步
- 已把构建留在 `captureInput=false` 并装机（Roleplay-Hub-1.0-debug.apk），等待人工中文输入测试。
- 若中文恢复正常且按键回归可接受 → 下一步下线前端代理桥、补工坊 composition 守卫（P1 清理）。
- 若中文仍不正常或回归不可接受 → 改回 `true` 并走 P1 补丁路线（改回命令：把 `capacitor.config.json` 中 `"captureInput": false` 改为 `true`，再 `npm run android:debug` 装机）。

## 9. 交互调整：回车改换行（2026-08-04）

用户确认 P0 结果：中文输入恢复正常、按键回归可接受。随后要求"回车不是发送，改成换行"。

### 改动
- `assets/js/app.js` `handleChatInputKeydown`：删除"回车直接发送"逻辑；
  - 普通 **Enter** → 交给 textarea 默认行为**插入换行**（不再发送）；
  - **Shift+Enter** → 同样插入换行；
  - **Ctrl/Cmd+Enter** → 发送（保留键盘发送通道）；
  - 保留 IME 组合守卫（`isComposing / chatInputComposing / keyCode 229` 时不拦截，避免候选词回车误触发）。
- `tests/chat-input-ime-contract.test.mjs`：新增契约测试"Enter 换行、Ctrl/Cmd+Enter 发送"，51/51 通过。

### 真机验证（captureInput=false 构建）
- Enter：`abc` + Enter → textarea 值 `"abc\n"`，消息数不变，高度自动增高（autoResize 正常）；
- Ctrl+Enter：`def` + Ctrl+Enter → 输入框清空、消息发送成功。

### 说明
- 手机软键盘没有 Ctrl 键，移动端发送走右侧发送按钮（正常 IM 惯例）。
- 角色工坊"描述你想创建的角色"的 prompt 仍是 Enter 生成，未改动（属另一流程）。