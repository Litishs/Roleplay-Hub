# 🍊妈妈们卡片全面审查与完善方案报告

> 审查对象：`assets/character/🍊妈妈们是不可能同时成为你的爱人_炮架_老师的.png`
> （含上一轮生成的 `_修复版.png`，两者除 UI 模板脚本点击逻辑外数据一致）
> 审查日期：2026-08-05
> 审查方法：解析 PNG 内嵌 `chara` 数据，逐字段核对世界书、正则、UI 模板、变量结构，并结合宿主（RolePlay Hub）的 UI 模板渲染与变量更新机制做兼容性分析。

---

## 一、结论摘要

这张卡的**世界观内容（世界书）质量不错**，但**交互层（UI 模板面板）是一个没有接上世界书的状态展示壳**，存在两类核心缺陷：

1. **阶段状态机缺失**：世界书为每个核心角色定义了 5 个感情阶段（按“感情进度 0-20% / 20-40% / 40-60% / 60-80% / 80-100%”划分），但面板里的“感情阶段”是写死的静态字符串，没有任何“状态变化 → 阶段推进”的计算逻辑。好感度、性接受度、兴奋值怎么变，阶段都纹丝不动。
2. **状态不回写、切走即丢**：面板维护了一份模板内部静态数据（`characterDatabase`，林夕瑶好感度写死 15），运行时的一切修改都只改显示、从不写回；切换角色时又从静态数据重新取值覆盖显示。这就是“好感度改成 40 后，切走再切回又变回 15”的根因。
3. **缺少“上下文驱动”的交互层**：对照参考卡（`想打姐姐屁股🍇`），这张卡没有“根据当前剧情给出可点击的行动建议”能力。参考卡的实现是“宿主变量分析器每轮按 `variableSchema` 维护上下文状态和建议变量，模板渲染按钮，点击后通过 `window.triggerSlash('/send …')` 把行动发回剧情”——这套机制才是“与上下文发展保持一致”的正确范式，本卡目前完全缺失（详见第七、八节）。

此外还发现一处**世界书内容复制粘贴错误**（夏芷萱的阶段系统整段写成了苏婉晴的）、模板变量结构与宿主变量系统不适配、以及若干低优先级问题。详见下文。

---

## 二、卡片资产概况

| 项目 | 现状 |
|---|---|
| 格式 | Character Card V2（`spec: chara_card_v2` / `2.0`），解析正常 |
| 卡内名称 | `我那个只大两岁的小妈居然要和我住一起`（与文件名不一致） |
| 描述/人设 | description 164 字；personality 882 字；first_mes 13421 字 |
| scenario | 有（深夜换衣撞见场景） |
| 世界书 | 14 条，全部 `enabled: true, constant: true`（常驻注入） |
| 正则脚本 | 6 条，均为显示类（markdownOnly） |
| UI 模板 | 1 个（“互动状态面板”，placement=bottom，约 14-15KB） |
| 变量 | 扁平结构：weekday / location / char_name / outfit / favor / accept / excitement / stage / mental / attitude |
| variableSchema | **空字符串**（宿主变量分析器无约束） |
| 其他 | mes_example、alternate_greetings、tags、system_prompt 均为空 |

世界书结构：

- 样式规范（对话格式要求：心理活动用（），对白用『』）
- 林夕瑶 / 苏婉晴 / 夏芷萱 三人的“感情进度与阶段反馈系统”（各 5 阶段）
- 三人的“全阶段思维链与反馈系统”
- 林夕瑶专属“感情进度与阶段反馈系统”
- 父亲、苏清颜、沈曼君、柳玉茹 的人物设定
- 管家、女仆们无独立世界书条目（面板里统一显示“无感情线”）

世界书定义的 5 阶段（按“感情进度”百分比划分，每档 20%）：

| 角色 | 阶段1 | 阶段2 | 阶段3 | 阶段4 | 阶段5 |
|---|---|---|---|---|---|
| 林夕瑶 | 初识拘谨期 | 试探放松期 | 暧昧拉扯期 | 心动沉沦期 | 越界沉沦期 |
| 苏婉晴 | 长辈关照期 | 熟络亲近期 | 克制心动期 | 边界松动期 | 越界相守期 |
| 夏芷萱 | 打趣调侃期 | 熟络打闹期 | 吃醋傲娇期 | 主动撩拨期 | 越界占有期 |
| 苏清颜 | 愧疚疏离期 | 试探靠近期 | 心疼依赖期 | 挣扎沉沦期 | 隐秘相守期 |
| 沈曼君 | 慈爱长辈期 | 习惯依赖期 | 心动恐慌期 | 痛苦挣扎期 | 隐秘相守期 |
| 柳玉茹 | 护短姐姐期 | 意识模糊期 | 心动迷茫期 | 纠结挣扎期 | 隐秘相守期 |

---

## 三、核心问题清单

### P0-1 阶段状态机缺失：阶段永远不随状态变化

**现象**：好感度 +25 变 40，面板显示 40，但“感情阶段”仍是“阶段1：初识拘谨期”。

**根因**：面板的 `stage` 只是 `characterDatabase` 里写死的字符串。模板脚本里与阶段相关的代码只有三处：

```js
// characterDatabase 每个角色硬编码，例如：
linxiyao: { favor: 15, stage: "阶段1：初识拘谨期", ... }
// switchCharacter 里原样搬运：
window.stage = data.stage;
// refreshPanel 里原样显示：
setText('rph-stage', d.stage);
```

没有任何“favor/accept/excitement → 感情进度 → 阶段”的计算逻辑。世界书定义了 5 阶段和百分比阈值，面板完全没用上。甚至 `沈曼君` 好感度 90 也写死“阶段1”，可见阶段与数值从来就没有联动过。

**验证**：全文检索 `stage`/`favor`，除数据定义与显示搬运外，无任何计算/比较/分支。

### P0-2 状态不回写：切换角色后数值回退

**现象**：好感度改成 40（宿主变量系统里确认已是 40），切到别的角色再切回，面板显示回到 15。

**根因**：模板维护一份内部静态数据 `window.characterDatabase`（林夕瑶 `favor: 15` 写死在卡里）。`switchCharacter()` 每次切换都从这份常量重新取值，覆盖 `window.favor` 和面板 DOM；**整个脚本没有任何一处把修改后的值写回 `characterDatabase`**。

于是出现两套互相打架的状态：

| 状态来源 | 好感度 |
|---|---|
| 宿主变量系统（你手动改的） | 40 |
| 模板内部 characterDatabase（切回时重新读） | 15 |

面板显示由模板内部数据接管，所以切回显示 15；宿主变量系统仍是 40。这也是上一轮报告里提到的“两套状态体系”问题，这次被实测坐实。

**补充**：即使模板内部做内存写回，每次宿主重新渲染面板（如新消息后重挂模板块）脚本都会重新执行、重新把 `characterDatabase` 初始化成默认值——所以改进时必须加“初始化保护”（已有运行时状态则不覆盖）。

### P1-1 世界书内容张冠李戴（复制粘贴错误）

世界书第 7 条 `夏芷萱专属·感情进度与阶段反馈系统`，内容整段是**苏婉晴**的：

- 内容开头写“本系统为**苏婉晴**专属…温柔知性、通透佛系、成熟克制”
- 阶段列表是 长辈关照期 → 熟络亲近期 → 克制心动期 → 边界松动期 → 越界相守期（与苏婉晴完全相同）

而夏芷萱正确的阶段（见第 10 条“夏芷萱全阶段·专属思维链”）是：打趣调侃期 → 熟络打闹期 → 吃醋傲娇期 → 主动撩拨期 → 越界占有期。

这会导致 AI 按苏婉晴的阶段规则去演绎夏芷萱，人物行为会跑偏。

### P1-2 模板变量结构与宿主变量系统不适配

- `initialVariableState` 是**扁平、单角色**结构（favor/accept/excitement/stage… 各一份），但卡里有 13 个角色、每个角色一套状态。面板内切角色只是脚本改显示，宿主变量系统永远只存“最后显示的”那套。
- `variableSchema` 为空字符串，宿主的变量分析器（RolePlay Hub 里是独立 AI 分析模型）没有任何字段说明，容易更新错变量或拒绝更新。
- 面板没有“感情进度”变量，而世界书恰恰用“感情进度”定义阶段——变量体系与世界书体系脱节。

### P1-3 面板“切换角色”不联动真实对话角色

面板的 13 个角色选项只切换**面板展示**（头像信息、数值），不会切换当前对话的角色。在 RolePlay Hub 中模板能用的宿主桥只有 `window.triggerSlash`（发一条用户消息并触发回复），没有“切换当前角色”的接口。原版网页若支持整卡切换，则是其宿主能力，不是这张卡自带的。

### P2-1 正则脚本复核项

1. `正文阅读面板`：`^([\s\S]+)$` 匹配整条消息并整体包一层大 div。属于全量包裹型正则，与其它 5 条显示正则叠加时顺序敏感，建议实测长消息、含代码块/HTML 的消息是否有双重包裹或样式冲突。
2. `林夕瑶标签`、`小妈标签` 用了 `\b(林夕瑶|夕瑶)\b`、`\b(小妈)\b`。JS 的 `\b` 只认 ASCII 单词字符，对中文边界**不生效**（中文两边都是非单词字符，`\b` 会匹配在中文旁，效果近似于无界），建议改为 `(?<![林夕瑶小妈])…` 这类中文自定义边界或直接去 `\b`。

### P2-2 元数据缺失与命名不一致

- 卡内名称与文件名不一致（文件名“妈妈们…炮架_老师”，卡内“我那个只大两岁的小妈…”）。
- `mes_example`、`alternate_greetings`、`tags`、`system_prompt` 均为空，导出到其它平台时缺少示例对话与标签。
- `creator: AI CharGen`、`character_version: 0.9`，属生成稿。

### P3 世界书常驻注入的 token 开销

14 条全部 `constant: true`（无条件常驻），其中多条是几千字的完整设定。按每条都在每次请求注入计算，token 消耗偏高，可评估部分低频条目改为非 constant 或提高触发门槛。

### 已修复项（上一轮）

| 问题 | 状态 |
|---|---|
| 点击角色选项无反应（Shadow DOM 委托下裸 `event.currentTarget` 抛 TypeError） | 已修复（`_修复版.png`） |
| `window.location = …` 会触发整页跳转 | 已修复（移除） |
| 依赖宿主不存在的 `window.refreshUI` | 已修复（模板内 `refreshPanel()` 直接刷新 DOM） |

---

## 四、与你两个观察现象的对应

| 你观察到的现象 | 对应问题 | 说明 |
|---|---|---|
| 好感度 40，感情阶段还是阶段一 | P0-1 | 阶段是静态字符串，代码里根本没有“状态 → 阶段”的计算 |
| 切走再切回，好感度回到 15 | P0-2 | 模板内部数据是写死的 15，切换时重新读取覆盖显示；修改从未写回 |
| 变量系统里确实是 40 | P1-2 | 宿主变量系统与模板内部数据是两套状态，互不同步 |

两个现象都在**卡模板内部**，与宿主无关，所以你在原始网页版测试结果一致。

---

## 五、完善方案

### 方案 A：卡片自洽（推荐先做，不需要改宿主）

目标：让面板自己成为一套完整、一致、会话内不丢的状态机。所有改动都在卡的 UI 模板脚本内。

**A1. 状态结构（内存态，写回）**

```js
// 每个角色一份运行时状态，初始化只执行一次：
if (!window.characterDatabase) {
    window.characterDatabase = { linxiyao: { favor:15, accept:0, excitement:0, progress:15, stage:1, ... }, ... };
}
// 任何数值变化都写回：window.characterDatabase[charId].favor = 40;
// 切换角色：从 characterDatabase 读，而不是从常量重新初始化
```

要点：初始化加保护（`if (!window.characterDatabase)`），避免宿主每次重渲染面板时把已修改的状态重置回默认值；切走再切回、甚至面板重渲染，数值都不丢。

**A2. 阶段状态机（把世界书接上）**

- 新增“感情进度” `progress`（0-100），由三项状态加权计算，默认权重建议：
  `progress = clamp(round(favor*0.60 + accept*0.25 + excitement*0.15), 0, 100)`
- 按世界书的 5 档阈值映射阶段：`0-20 → 阶段1，20-40 → 阶段2，40-60 → 阶段3，60-80 → 阶段4，80-100 → 阶段5`
- 每个角色配一套阶段名（直接抄世界书的表，修正后的），替换现在写死的单个 stage 字符串
- “较大变化才更新”的实现：阶段只在 `progress` **跨过阈值**时推进（默认只进不退，可配置）；也可以再加一个最小变化量门槛（如累计变化 ≥5 才重算），避免细微波动导致阶段抖动
- 面板增加“感情进度”进度条，并在阶段变更时做高亮提示（视觉反馈“状态有较大变化”）

**A3. 世界书修正**

- 修正第 7 条夏芷萱的阶段系统（改为 打趣调侃期 → 熟络打闹期 → 吃醋傲娇期 → 主动撩拨期 → 越界占有期，与第 10 条一致）

**A4. 面板增强（可选）**

- 数值变化来源可视化：面板上每个角色旁显示三项状态的当前值 + 感情进度
- 阶段变更时短暂高亮阶段徽章，让“状态有较大变化 → 阶段更新”的联动可见

方案 A 的局限：状态只在**当前页面会话**内有效；刷新页面/重新打开对话后，回到卡里的初始值（因为没有同步到宿主持久化变量）。

### 方案 B：宿主联动（根治，需要改 App + 改卡）

目标：模板状态与宿主变量系统真正打通，刷新、换会话、回看历史都一致。

**B1. 变量结构改成按角色嵌套**（宿主可持久化多角色状态）：

```json
{
  "global": { "weekday": "周六", "location": "你的卧室" },
  "characters": {
    "linxiyao": { "char_name": "...", "outfit": "...", "favor": 15, "accept": 0, "excitement": 0, "progress": 15, "stage": 1 },
    "suwanqing": { ... }
  }
}
```

模板里的 `{{char_name}}` 等改为 `{{characters.linxiyao.char_name}}` 这类嵌套路径（RolePlay Hub 的变量渲染已支持点路径）。

**B2. 补全 variableSchema**：声明每个角色的字段类型与取值范围，宿主的变量分析器（AI）就不会乱改或漏改。

**B3. 增加模板 → 宿主写回桥**：

- RolePlay Hub 侧：在 `ui-template-frame.js` 的 shimWindow 上新增 `rphSetUiTemplateVariables(updates)`，由 `app.js` 注册实现，内部调用 `setUiTemplateValue` 写入 `template.variableState` + `changeLog` 并重挂面板。
- 模板侧：`switchCharacter` / 数值更新时同时调用该桥同步宿主。
- 原始网页版：按其暴露的宿主 API 对接（原版如有 refreshUI 一类的宿主钩子，写回其变量存储即可）。

**B4. 面板“切换角色”是否联动真实对话角色**：这是宿主能力问题。若宿主提供角色切换接口，模板脚本可一并调用；没有则保持纯展示并明确标注“仅查看”。

### 实施顺序建议

1. 先做方案 A（纯卡内改动，不动 App）——解决你实测的两个现象，立即可导入使用
2. 同时修正世界书第 7 条张冠李戴
3. 若需要跨会话/刷新持久化，再上方案 B（需要改 App 侧桥 + 重新导出卡）
4. 低优先级：正则边界修正、元数据补全、constant 注入优化

---

## 七、参考卡实现分析：想打姐姐屁股🍇

**机制一句话**：这张卡的“剧情建议”不是卡内脚本现场生成的，而是**宿主变量分析器每轮按 `variableSchema` 规则把“建议行动”写进变量**，模板渲染成按钮；用户点击后模板校验指令，并调用 `window.triggerSlash('/send …')` 把行动作为用户输入发回剧情，触发下一轮回复。

整张卡分成三层：

### 7.1 数据层：宿主 AI 每轮维护上下文状态

`variableSchema` 定义了 40+ 个字段及其更新纪律，例如：

```json
"_update_rules": "每轮仅依据最近完成的正文和当前变量做merge更新；未知字段保持上一轮，不把推测伪装成事实，不得编辑HTML。地图字段必须与正文地点一致；current_situation使用第三人称、游戏CG式细腻描写，只写当前一刻……三个action_*_label必须是互不重复、符合上下文和角色性格的玩家建议行动；对应action_*_command必须以'/send '开头、单行、总长不超过180字符，只提交意图，不替模型判定结果。"
```

字段分两类：

- **上下文状态**：`current_situation`（当前情境）、`scene_kind`（场景类型，受限取值）、`present_characters` / `third_party_names`（实际在场人物，明确“电话另一端/被提及姓名/离屏行动者不得列入”）、`current_location` / `available_routes`（地点与可达路线）、`latest_event`、`weather`、`relationship_phase`、`character_name`、`emotion`、`clothing`、`special_items`、`inner_tip`、身体状态字段（`breast/anus/vagina/uterus_state`，要求“没有上下文依据时明确写未判定，禁止凭空制造接触/高潮/伤情”）。
- **建议行动**：`action_1_label` / `action_1_command` … `action_3_label` / `action_3_command`。

在 RolePlay Hub 里对应的就是 `updateUiTemplatesFromChat` 流程：每轮回复完成后，把“最近对话 + 当前变量 + variableSchema”发给分析模型，返回 JSON 更新变量，然后重挂面板。

### 7.2 展示层：模板按变量渲染

```html
<button type="button" class="context-action" data-index="01"
        data-command="{{action_1_command}}">{{action_1_label}}</button>
<!-- …共 3 个… -->
```

上下文一致性还有一层硬约束：模板主容器把关键上下文放在 `data-*` 属性上，脚本读取后决定功能是否可用：

```html
<main class="rp-shell" id="app"
      data-user-name="{{user}}"
      data-scene-kind="{{scene_kind}}"
      data-present-characters="{{present_characters}}"
      data-scene-context-evidence="{{scene_context_evidence}}"
      data-lock-reason="{{game_lock_reason}}">
```

脚本 `analyzeContext()` 据此判断当前场景（如户外或检测到第三人在场）并锁定不适合展开的玩法（牌局），实现“面板行为随剧情场景走”。

### 7.3 交互层：点击 → 校验 → triggerSlash

```js
function dispatchSlash(command) {
  if (!validSlash(command)) { showToast('行动指令未通过安全校验。'); return false; }
  if (typeof window.triggerSlash !== 'function') { showToast('当前宿主没有开放 triggerSlash；请手动输入该行动。'); return false; }
  window.triggerSlash(command);  // 作为用户消息发回剧情并触发下一轮
}
```

校验规则：以 `/send ` 开头、单行、总长 ≤ 520 字符；失败有 toast 兜底。此外该卡还带一个本地小游戏（牌局，`localStorage` 存档），结算时通过 `/send 【绯夜牌局结算】…` 写回剧情。

### 7.4 可复用模式提炼

| 能力 | 实现方式 |
|---|---|
| 剧情建议 | 宿主分析器每轮维护 `action_N_label/command` 变量 → 模板按钮 → 点击触发 |
| 与上下文一致 | 宿主按 schema 纪律维护情境/在场人物/地点/状态变量；模板据此渲染与锁定 |
| 用户触发交互 | 校验 `/send` 指令 → `window.triggerSlash` 发回剧情 |
| 状态纪律 | “只依据最近正文 merge、未知保持、不把推测当事实”写在 schema 里约束分析器 |

---

## 八、🍊妈妈们卡整合完善方案（把参考卡能力移植过来）

目标三条：**与上下文发展保持一致、清晰展现角色状态、交互体验好**。方案 C 是前面 A/B 的整合版，推荐按此实施。

### C1. 状态与上下文一致性（核心）

- 变量结构改为：
  ```json
  {
    "global": { "current_time": "…", "current_location": "…", "current_situation": "…",
                "present_characters": "…", "latest_event": "…" },
    "characters": {
      "linxiyao": { "char_name": "…", "outfit": "…", "favor": 15, "accept": 0,
                    "excitement": 0, "progress": 15, "stage": 1,
                    "mental": "…", "attitude": "…" },
      "suwanqing": { … }, …
    }
  }
  ```
- 写一份完整 `variableSchema`，规则参照参考卡 + 本卡世界书：
  - 每轮只依据最近完成的正文 merge 更新；未知字段保持上一轮；不把推测伪装成事实；不得编辑 HTML
  - 当前互动角色的 `favor/accept/excitement` 按正文实际变化更新；`progress` 由三项加权计算；`stage` 只在跨阈值时推进（只进不退，规则可配置）
  - `current_situation` / `present_characters` / `current_location` 必须与正文一致（参考卡的“在场人物纪律”直接照搬）
  - 三个建议变量规则与参考卡一致（互不重复、符合当前角色/阶段/情境、`/send` 开头、单行、≤180 字符、只提交意图）
- 状态唯一来源在宿主变量里，模板不再维护私有 `characterDatabase`——“切走再切回”“刷新”“回看”都一致。

### C2. 清晰的角色状态展示（面板升级）

- 每个角色展示：三项数值条 + **感情进度条（新增）** + 阶段徽章 + 当前情境一句话 + 当前地点/在场人物
- 阶段变更时高亮徽章，让“状态有较大变化 → 阶段更新”可见
- 面板底部小字展示“上下文依据”（参考卡 `scene_context_evidence` / `inner_tip` 的做法），例如“依据：两人在卧室独处，林夕瑶被看到脸红”
- 面板切换角色时从 `characters.<id>` 读取渲染，切换不丢状态

### C3. 交互体验（建议按钮）

- 面板底部加 3 个“下一步行动”按钮：
  ```html
  <button class="context-action" data-command="{{action_1_command}}">{{action_1_label}}</button>
  ```
- 点击流程照搬参考卡：校验 `/send` 前缀/单行/长度 → `window.triggerSlash(command)` → 作为用户输入触发下一轮
- 失败兜底 toast；建议变量未更新时按钮置灰并提示“等待上下文分析”

### C4. 配套修正（沿用前几节）

- 世界书夏芷萱条目修正（第 7 条张冠李戴）
- 面板阶段名与修正后的世界书统一（抄第 6-13 条定义）
- 正则 `\b` 中文边界修正
- 可选：App 侧新增模板 → 宿主变量写回桥（`rphSetUiTemplateVariables`），让面板切换/数值即时同步宿主，不必等下一轮分析

### 实施边界与风险

- 建议按钮依赖宿主每轮跑变量分析：RolePlay Hub 的 `updateUiTemplatesFromChat` 已具备；原始网页版需确认其分析器同样按 schema 更新，否则建议会停留在初始值
- 多角色嵌套状态会让变量 payload 变大，字段要控制数量与长度（参考卡单角色 40+ 字段已是上限量级）
- 面板“切换角色”是否联动真实对话角色仍是宿主能力问题，需确认（RolePlay Hub 目前只能展示，不能切换对话角色）

### 实施顺序建议

1. C1 变量结构 + variableSchema（核心，先做）
2. C2 面板升级（进度条、情境、依据展示）
3. C3 建议按钮 + triggerSlash（交互）
4. C4 配套修正（世界书、正则）
5. 可选：App 写回桥

---

## 九、需要你确认的决策点

1. **感情进度公式**：三项状态各占多少权重？（建议 好感度 60% / 性接受度 25% / 兴奋值 15%，可调）
2. **阶段推进规则**：只进不退，还是允许回退？是否加“最小变化量”门槛？
3. **面板“切换角色”**：是否要求联动真实对话角色？（涉及宿主能力，RolePlay Hub 目前不支持，需要加宿主功能）
4. **建议按钮**：数量固定 3 个？`/send` 指令风格（只提交意图，还是允许“我试着…然后…”式完整动作）？
5. **本次实施范围**：按方案 C 全做（变量结构 + schema + 面板升级 + 建议按钮 + 世界书/正则修正），还是分步做（先 C1+C2，再加 C3）？
6. **是否同时改 App 加写回桥**（让面板操作即时同步宿主变量，需动 App 代码并回归测试）。

确认后我可以直接产出：修正后的 `_修复版` 卡片（含状态机、建议按钮、世界书修正），以及（如需）App 侧的写回桥改动。
